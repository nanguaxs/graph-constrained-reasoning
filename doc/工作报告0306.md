# 工作报告 - 2026年3月6日

## 1. 直接在Prompt中加上约束开始标签，无需微调规范格式

### 1.1 核心改进
通过在 prompt 末尾直接添加 `<PATH>` 标签，实现了无需微调即可触发图约束解码的方案：

- **新增中文 Prompt 类**：`ChinesePathGenerationWithAnswerPromptBuilder`
- **Prompt 结构**：以 `推理路径：<PATH>` 结尾，直接触发约束生成
- **约束机制**：`check_constrained_flag` 扫描整个输入序列（包括 prompt 部分）定位 `<PATH>` 标签

### 1.2 Chat Template 兼容性修复
解决了 chat template 破坏 prompt 末尾 `<PATH>` 标签的问题：

- **问题**：`<PATH>` 被夹在 user 和 assistant 回合之间，导致约束失效
- **修复方案**：检测 query 末尾的 `<PATH>`，先移除再应用 chat template，最后追加到 assistant 回合开头
- **最终结构**：`<|im_start|>assistant\n<PATH>`，确保约束正确触发

### 1.3 特殊 Token 处理优化
- **推理时自动添加**：在 tokenizer 加载后立即添加 `<PATH>` 和 `</PATH>` 特殊 token
- **ID 一致性**：确保推理时 token ID 与微调时保持一致
- **无需模型修改**：只需 tokenizer 识别，不依赖 embedding 层


# 图约束路径生成 GRPO 微调方案

## 第一部分：核心架构设计

传统强化学习（如 PPO）容易崩溃，且显存开销大（通常需要同时加载 4 个模型）。本方案采用当前推理任务主流范式：**GRPO**。

### 1.1 基座模型
- 建议模型规模：**7B/8B Instruct**（如 `Qwen2.5-7B-Instruct`、`Llama-3-8B-Instruct`）
- 训练方式：**LoRA/PEFT**
- 在 A800 80GB 上显存安全余量充足

### 1.2 GRPO（群体相对策略优化） 核心机制
- **不训练独立 Critic（价值网络）**，直接降低显存占用与训练复杂度
- **相对打分**：
  - 针对同一问题，一次生成 `G` 条路径（建议 `G=4`）
  - 对组内路径打分并标准化，得到优势值（Advantage）
  - 高分路径概率上调，低分路径概率下调

### 1.3 约束与探索融合策略
- **Rollout（探索生成）阶段**：注入 `prefix_allowed_tokens_fn`
  - 保证生成路径 **100% 图谱合法**
- **Update（梯度更新）阶段**：撤销约束
  - 让损失函数直接作用于模型底层概率分布

---

## 第二部分：数据准备方案

无需复杂 JSONL 对齐逻辑，可在内存中构建 PyTorch Dataset，输出两类信息：

### 2.1 输入指令（Prompt）
将 `question` 与 `q_entity` 组织为标准对话模板（Chat Template）。

建议结构：
- 系统设定：你是知识图谱导航员
- 任务描述：从起点实体出发，推理可回答问题的目标实体
- 用户问题：`question`
- 起点实体：`q_entity`
- 输出：`推理路径:`

### 2.2 验证元数据
该数据不输入模型，仅用于奖励函数计算。

建议字段：
- `a_entity`（真实答案/目标终点实体）


> 因为 `prefix_allowed_tokens_fn` 已保障结构合法，奖励函数可不再处理“路径是否在图谱内”的校验逻辑。

---

## 第三部分：奖励函数极简设计

奖励函数只关注：**语义相关度 + 推理效率**。

对同一问题生成的 `G` 条路径分别打分：

### 3.1 终极目标命中奖
- 提取路径终点节点 `Final Node`
- 若 `Final Node == a_entity`：奖励 `+10.0`

### 3.2 语义接近得分
- 若未命中终点，不直接置 0，避免梯度稀疏
- 使用轻量向量模型（如 `BGE-m3`）计算终点与 `a_entity` 余弦相似度
- 奖励建议：`sim * 3.0`

### 3.3 绕路惩罚
- 若路径出现绕圈或明显过长，按 hop 惩罚
- 建议：超过预期 hop 后，每多一跳 `-0.5`



---

## 第四部分：A800（80GB）推荐参数

### 4.1 LoRA 参数
- `r = 64`
- `alpha = 128`
- `target_modules = [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]`

### 4.2 GRPO / 生成参数
- `num_generations (G) = 4`
- `max_new_tokens = 64 ~ 128`
- `temperature = 0.8 ~ 0.9`

> 温度不能太低，否则组内样本同质化，优势值退化。

### 4.3 优化器与训练参数
- `learning_rate = 1e-6 ~ 3e-6`
- `per_device_train_batch_size = 1`
- `gradient_accumulation_steps = 8`（等效 batch size = 8）
- `kl_penalty_beta = 0.04`

> 强化学习阶段学习率必须显著低于 SFT 常用学习率，避免 KL 爆炸。

---



---

## 最终预期收益

### 绝对无结构幻觉
通过 Trie + `prefix_allowed_tokens_fn`，模型无法输出图谱外 token 路径。

### 高命中率
通过 GRPO 的相对偏好优化，模型在多个合法分支中更倾向选择能到达正确答案终点的路径。

即：
- 不是“会不会走图谱”的问题（已由约束保证）
- 而是“在合法分支里选哪条更对”的问题（由 GRPO 学习解决）

#  进一步工作建议

1. **适配新版本 Transformers 与新模型（Qwen3.5 等）**
   - 设计并完善与新版本 Transformers 的兼容逻辑
   - 验证 `prefix_allowed_tokens_fn` 在新模型上的行为一致性
   - 完成关键推理链路回归测试（chat template、特殊 token、beam/group-beam）

2. **性能优化**
   - 基于当前方案，尝试 GRPO 强化学习，提升路径选择准确性

