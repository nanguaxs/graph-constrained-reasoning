## qa_prompt_builder.py

`qa_prompt_builder.py` 是构造 prompt 的地方。根据不同的参数，选择不同的 prompt 类：

- `GraphConstrainedPromptBuilder`：基类，初始化时调用 `get_prompt_template` 根据 `prompt` 参数名动态查找对应的 prompt 模板（如 `ZERO_SHOT_PROMPT`）。通过 `process_input` 函数生成三个返回值：
  - `input`：填充好的 prompt 文本
  - `ground_paths`：从问题实体到答案实体的真实最短路径，用于评估
  - `trie`：由图中所有路径构建的 MarisaTrie 前缀树，用于约束解码

- `JointReasoningPromptBuilder`：继承自基类，重写了 `get_graph_index` 方法构建前缀树，区别在于路径字符串会被 `<PATH>` 和 `</PATH>` 标签包裹，使模型输出的路径有明确的起止标记，便于后续解析。

- `PathGenerationWithAnswerPromptBuilder`：继承自 `JointReasoningPromptBuilder`，只替换了 prompt 模板，专用于"生成路径并回答问题"的任务场景。

## 为什么模型能识别 `<PATH>` 为特殊标签

因为微调时调用 `tokenizer.add_special_tokens()` 将 `<PATH>` 和 `</PATH>` 添加为特殊 token：

- 给它们分配新的 token ID（151646 和 151647）
- 扩展模型的 embedding 层以容纳新 token
- 保存 tokenizer 配置到模型目录

这样推理时加载同一个 tokenizer，`<PATH>` 就会被识别为单个 token 而不是 `<`、`PATH`、`>` 三个 token，Trie 中存储的 token 序列与模型生成的 token 序列能够完全对齐，约束解码才能正确工作。

## generate_sentence

`generate_sentence` 不是 transformer 自带的函数，而是项目自定义的方法，在 [graph_constrained_decoding_model.py:8-44](src/llms/graph_constrained_decoding_model.py#L8-L44) 中实现。它在 HuggingFace 的 `model.generate()` 基础上封装了图约束解码逻辑，接收 trie、`start_token_ids`、`end_token_ids` 等参数，通过 `GraphConstrainedDecoding` 作为 logits processor 注入到生成过程中，使模型只能生成 Trie 中存在的路径序列。

## enable_constrained_by_default 为 True 时，为全程约束状态吗

不完全是，要分两种情况：

**情况一：`start_token` 和 `end_token` 都为 `None`**（[graph_constrained_decoding.py:36-39](src/graph_constrained_decoding.py#L36-L39)）

`enable_constrained_by_default=True` 才真正生效，此时 `constrained_flag` 始终为 `True`，从生成第一个 token 起就全程约束。

**情况二：`start_token` 和 `end_token` 都有值**（[graph_constrained_decoding.py:33-34](src/graph_constrained_decoding.py#L33-L34)）

```python
if self.start_token is not None and self.end_token is not None:
    constrained_flag, L_input = self.check_constrained_flag(sent)
```

`constrained_flag` 会被 `check_constrained_flag` 的返回值直接覆盖，`enable_constrained_by_default` 的初始值完全被忽略，约束与否只取决于当前是否在 `<PATH>...</PATH>` 区间内（[graph_constrained_decoding.py:14-27](src/graph_constrained_decoding.py#L14-L27)）。

所以实际上这个参数只在不使用 `<PATH>` 标签的纯路径生成模式下有意义，联合推理模式下传什么值都无所谓。

## allow_tokens 为空时的 fallback 逻辑

见 [graph_constrained_decoding.py:44-45](src/graph_constrained_decoding.py#L44-L45)：

```python
if len(allow_tokens) == 0:
    return self.all_tokens
```

这段代码是约束解码的兜底 fallback。触发条件是第 43 行的 `trie.get()` 返回空列表：

```python
allow_tokens = self.trie.get(sent.tolist()[L_input:])
```

`sent[L_input:]` 是模型在约束区间内已生成的 token 序列，`trie.get()` 用它在前缀树中查找下一步允许的 token。返回空列表说明当前前缀在 trie 中已无任何后续路径，有两种根本原因：

1. **生成走进了死胡同** — 模型在某一步生成了 trie 中不存在的 token，导致前缀匹配断裂，后续所有路径都找不到。
2. **trie 未覆盖当前前缀** — 知识图谱中本身就没有以当前已生成序列为前缀的实体或路径。

此时返回 `self.all_tokens` 意味着退回到无约束模式，允许模型自由生成，防止生成过程卡死。这是防御性设计，但如果频繁触发，说明 trie 覆盖率不足或模型在约束区间内偏离了合法路径。

## k 值与生成模式

`k` 和 `generation_mode` 共同决定模型为每个问题生成多少条候选路径，以及用什么策略生成。定义在 [base_hf_causal_model.py:93-121](src/llms/base_hf_causal_model.py#L93-L121)。

| generation_mode | k 的作用 |
|---|---|
| `greedy` | k 无效，固定返回 1 条（贪心解码） |
| `sampling` | 随机采样，返回 k 条序列 |
| `beam` | 普通 beam search，维护 k 个 beam，返回 k 条 |
| `beam-early-stopping` | 同 beam，但找到足够序列后提前停止 |
| `group-beam` | Diverse Beam Search，k 个 beam 分成 k 组，组间施加 `diversity_penalty=1.0`，强制生成多样路径 |
| `group-beam-early-stopping` | 同 group-beam，加提前停止 |

脚本中使用 `generation_mode=group-beam`、`k=2`，对应如下配置：

```python
# base_hf_causal_model.py:108-113
self.generation_cfg.num_beams = k            # beam 总数
self.generation_cfg.num_return_sequences = k # 返回路径数
self.generation_cfg.num_beam_groups = k      # 分组数（每组 1 个 beam）
self.generation_cfg.diversity_penalty = 1.0  # 组间多样性惩罚
```

`k` 同时也会体现在输出目录名中（[predict_paths_and_answers.py:96](workflow/predict_paths_and_answers.py#L96)）：

```
results/GenPaths/COKG_QA/Qwen3-0.6B/train/zero-shot-group-beam-k2-index_len2/
```

## 新增 ChinesePathGenerationWithAnswerPromptBuilder 类

在 [qa_prompt_builder.py:200-216](src/qa_prompt_builder.py#L200-L216) 新增了一个中文 prompt 类，继承自 `JointReasoningPromptBuilder`，结构与 `PathGenerationWithAnswerPromptBuilder` 完全一致，区别是 prompt 使用中文，且末尾以 `推理路径：<PATH>` 结尾：

```python
class ChinesePathGenerationWithAnswerPromptBuilder(JointReasoningPromptBuilder):
    ZERO_SHOT_PROMPT = """推理路径是知识图谱中连接问题主题实体与答案实体的三元组序列。给定一个问题，请从主题实体出发，在知识图谱中生成若干推理路径。

# 问题：
{question}
# 主题实体：
{entities}
推理路径：<PATH>"""
```

## prompt 末尾的 `<PATH>` 是否会触发约束生成

会，且是正确的做法。关键在 `check_constrained_flag`（[graph_constrained_decoding.py:14-27](src/graph_constrained_decoding.py#L14-L27)）：

```python
# sent 包含 prompt token + 已生成 token
last_start_tokens = torch.where(sent == self.start_token)[0][-1]
# L_input 被设为 <PATH> 在 sent 中的位置
return True, last_start_tokens
```

然后在 `allowed_tokens_fn` 中：

```python
allow_tokens = self.trie.get(sent.tolist()[L_input:])
# 传入 trie 的是 [<PATH>_token_id] + 已生成的 token
```

trie 里存的路径以 `<PATH>` 开头，所以 `trie.get([<PATH>_token_id])` 正好返回路径内容的第一个合法 token。

**结论**：`check_constrained_flag` 会扫描整个 `sent`（包括 prompt 部分）来定位 `<PATH>`，因此 prompt 中的 `<PATH>` 同样能触发约束模式，模型生成的第一个 token 就会被约束为 trie 中的合法内容。

## chat template 会破坏 prompt 末尾的 `<PATH>`

`prepare_model_prompt` 在 chat 模式下会调用 `apply_chat_template`，导致 prompt 末尾的 `<PATH>` 被夹在 user message 和 assistant 回合之间：

```
...推理路径：<PATH><|im_end|>\n<|im_start|>assistant\n
```

此时 `check_constrained_flag` 找到 `<PATH>` 后，`sent[L_input:]` 实际上是 `[<PATH>, <|im_end|>, ...]` + 已生成 token，而 trie 里存的路径是 `<PATH>entity...`，前缀不匹配，trie 立刻返回空列表，退回无约束模式。

**修复**（[base_hf_causal_model.py:122-141](src/llms/base_hf_causal_model.py#L122-L141)）：检测 query 是否以 `<PATH>` 结尾，若是则先去掉再套 chat template，最后把 `<PATH>` 追加到 assistant 回合开头：

```python
if query.endswith(PATH_START):
    user_content = query[:-len(PATH_START)]
    chat_query = [{"role": "user", "content": user_content}]
    return self.tokenizer.apply_chat_template(...) + PATH_START
```

这样最终 prompt 结构为：

```
<|im_start|>user\n...推理路径：<|im_end|>\n<|im_start|>assistant\n<PATH>
```

`<PATH>` 位于 prompt 最末尾，`sent[L_input:]` 就是 `[<PATH>]` + 已生成 token，与 trie 完全对齐。

## generate_sentence 中的调试打印

在 [graph_constrained_decoding_model.py:26-39](src/llms/graph_constrained_decoding_model.py#L26-L39) 生成完成后添加了打印语句，用于调试时观察输入与输出：

```python
# 打印输入与模型原始输出（文本形式）
print("=" * 80)
print("【LLM输入】:")
print(llm_input)
print("=" * 80)
print("【模型原始输出 - 包含特殊token】:")
for idx, seq in enumerate(res.sequences):
    decoded_with_special = self.tokenizer.decode(seq[input_ids.shape[1]:], skip_special_tokens=False)
    print(f"Sequence {idx}: {repr(decoded_with_special)}")
print("\n【处理后输出 - 去除特殊token】:")
for idx, seq in enumerate(res.sequences):
    decoded_without_special = self.tokenizer.decode(seq[input_ids.shape[1]:], skip_special_tokens=True)
    print(f"Sequence {idx}: {decoded_without_special}")
print("=" * 80)
```

- `llm_input` 是经过 `prepare_model_prompt` 处理后的完整 prompt，可以直接看到 chat template 包装后的最终输入
- `seq[input_ids.shape[1]:]` 截掉 prompt 部分，只保留模型新生成的 token
- 两种解码方式对比：含特殊 token（`repr` 显示转义字符）vs 去除特殊 token（可读文本）

## allowed_tokens_fn 中的调试打印

在 [graph_constrained_decoding.py:46-68](src/graph_constrained_decoding.py#L46-L68) 对 `allowed_tokens_fn` 进行了修改，原函数整体注释保留，新函数新增两处调试打印：

```python
if constrained_flag:
    generated = sent.tolist()[L_input:]
    # 约束首次激活时打印（generated 只含 <PATH> 一个 token）
    if len(generated) == 1:
        print(f"[约束激活] L_input={L_input}, 前缀token={generated}, 解码='{self.tokenizer.decode(generated)}'")
    allow_tokens = self.trie.get(generated)
    if len(allow_tokens) == 0:
        # fallback 触发时打印当前前缀
        print(f"[Fallback] trie 无匹配，当前前缀='{self.tokenizer.decode(generated)}'，退回无约束")
        return self.all_tokens
```

- `[约束激活]`：`generated` 长度为 1 时说明刚进入约束区间，此时 `generated` 就是 `<PATH>` 的 token ID，打印一次确认约束是否被触发及 `L_input` 位置是否正确
- `[Fallback]`：trie 匹配失败时打印当前已生成的前缀，帮助定位是哪个 token 导致匹配断裂

## 推理时手动添加特殊 token

**问题根因**：微调时通过 `tokenizer.add_special_tokens` 将 `<PATH>` 和 `</PATH>` 加入词表，但推理时直接从原始模型路径加载 tokenizer，这两个 token 不存在，导致 `convert_tokens_to_ids('<PATH>')` 返回 `None`，`start_token` 和 `end_token` 均为 `None`，约束逻辑完全失效。

**修复**（[base_hf_causal_model.py:55-59](src/llms/base_hf_causal_model.py#L55-L59)）：在 `prepare_for_inference` 加载 tokenizer 后立即手动添加：

```python
self.tokenizer = AutoTokenizer.from_pretrained(...)
# 推理时手动添加特殊 token，与微调时保持一致，确保 <PATH> 和 </PATH> 有固定 token ID
self.tokenizer.add_special_tokens({'additional_special_tokens': ['<PATH>', '</PATH>']})
```

注意：此处只需让 tokenizer 认识这两个 token 并分配 ID，不需要调用 `model.resize_token_embeddings()`，因为推理时不依赖模型对这两个 token 的 embedding，只需要 token ID 能与 trie 中存储的路径前缀对齐即可。

## 约束结束后强制停止生成

**问题**：模型在生成 `</PATH>` 关闭约束区间后，`constrained_flag` 变为 `False`，`allow_tokens` 退回 `self.all_tokens`，模型会继续自由生成额外内容，产生冗余输出。

**修复**（[graph_constrained_decoding.py:13,28,32-33](src/graph_constrained_decoding.py#L13)）：引入 `self.completed` 标志，检测到约束关闭后立即只允许 EOS，强制模型停止。

三处改动：

1. `__init__` 初始化标志：
```python
self.completed = False
```

2. `check_constrained_flag` 的 else 分支（end_token 已出现）设置标志：
```python
else:
    self.last_start_token = None
    self.completed = True      # 新增
    return False, len(sent)
```

3. `allowed_tokens_fn` 开头提前拦截：
```python
if self.completed:
    return [self.tokenizer.eos_token_id]
```

状态流转：

```
completed=False → 约束中 → trie tokens
completed=True  → 约束结束 → [eos_token_id] → 停止
```

注意：`completed` 是实例级状态，多轮复用同一实例时需在每次新推理前手动重置 `self.completed = False`。

**方案一的缺陷（已修复）**：beam search 时所有 beam 共享同一个实例，某个 beam 率先生成 `</PATH>` 设置 `completed=True` 后，其他尚未生成 `</PATH>` 的 beam 也会被强制返回 EOS，导致输出末尾出现乱码（缺少 `</PATH>`）。

**方案二（当前实现）**（[graph_constrained_decoding.py:30-36](src/graph_constrained_decoding.py#L30-L36)）：去掉 `self.completed`，改为在 `allowed_tokens_fn` 开头基于每个 beam 自己的 `sent` 独立判断：

```python
if self.end_token is not None and self.start_token is not None:
    matched_start = torch.where(sent == self.start_token)[0]
    if len(matched_start) > 0:
        last_start = matched_start[-1]
        if len(torch.where(sent[last_start:] == self.end_token)[0]) > 0:
            return [self.tokenizer.eos_token_id]
```

每个 beam 的 `sent` 是独立的，只有当该 beam 自己生成了 `</PATH>` 后才返回 EOS，不影响其他 beam。

## 评估指标（eval_path_result_w_ans）

`eval_path_result_w_ans` 在 [qa_utils.py:466](src/utils/qa_utils.py#L466) 中实现，计算11个指标：

| 指标 | 预测输入来源 | Ground Truth | 计算逻辑 |
|------|------------|-------------|---------|
| Acc | `" ".join(prediction)` 原始全文 | `ground_truth` | 每个答案是否作为子串出现在全文中，返回命中比例 |
| Hit | `" ".join(prediction)` 原始全文 | `ground_truth` | 任意一个答案出现在全文中则返回 1，否则返回 0 |
| F1 | `predicted_ans`（`# Answer:\n` 之后的部分） | `ground_truth` | Precision 和 Recall 的调和平均：`2*P*R/(P+R)` |
| Precision | `predicted_ans` | `ground_truth` | 每条预测中包含任意答案的数量 / 预测总条数 |
| Recall | `predicted_ans` 拼接全文 | `ground_truth` | 所有预测拼接后的文本中包含的答案数量 / 答案总数 |
| Path F1 | `predicted_path`（`# Reasoning Path:\n` 之后、`# Answer:\n` 之前） | `ground_truth_paths` | 预测路径 vs 真实路径，Precision 和 Recall 的调和平均 |
| Path Precision | `predicted_path` | `ground_truth_paths` | 每条预测路径中包含任意真实路径的数量 / 预测路径总条数 |
| Path Recall | `predicted_path` | `ground_truth_paths` | 所有预测路径拼接后包含的真实路径数量 / 真实路径总数 |
| Path Answer F1 | `predicted_path` | `ground_truth` | 预测路径 vs 真实答案，Precision 和 Recall 的调和平均 |
| Path Answer Precision | `predicted_path` | `ground_truth` | 每条预测路径中包含任意答案的数量 / 预测路径总条数 |
| Path Answer Recall | `predicted_path` | `ground_truth` | 所有预测路径拼接后包含的答案数量 / 答案总数 |

- `ground_truth` 来自数据集的 `answer` 字段
- `ground_truth_paths` 由 `qa_prompt_builder.py` 中 `get_truth_paths` 动态从图中计算得出
- Acc/Hit 用全文子串匹配，比 F1 宽松，答案只要出现在推理路径中也算命中
- 如果预测字符串只有路径没有 `# Answer:\n` 标记，`predicted_ans` 退化为整条路径，F1/Precision/Recall 受影响，Acc/Hit 和路径类指标基本不变

## convert_cokg_qa.py 子图构建逻辑

子图构建采用混合策略，保证答案实体一定出现在子图中：

1. **路径搜索**（`find_shortest_paths_bfs`）：
   - 所有答案实体放入 `target_set`，BFS 对每个答案实体独立搜索路径
   - 最短路径 ≤ 2跳：枚举所有1跳和2跳路径，全部加入子图
   - 最短路径 > 2跳：用标准 BFS（带 `visited`）找一条最短多跳路径加入子图
   - 所有答案的路径三元组合并到同一个 set，去重后加入子图

2. **邻居扩展**（`extract_subgraph`）：
   - 问题实体扩展2跳邻居
   - **每个答案实体**各自独立扩展2跳邻居
   - 每个实体最多取200个邻居，所有结果合并到同一个 set

统计指标中的"平均路径长度"实际是路径三元组总数的均值（所有答案路径合并去重后的三元组数），不是跳数。

## get_truth_paths 的多路径行为

`get_truth_paths`（[graph_utils.py:78](src/utils/graph_utils.py#L78)）使用 `nx.all_shortest_paths` 枚举所有最短路径：

```python
for h in q_entity:
    for t in a_entity:
        for p in nx.all_shortest_paths(graph, h, t):
            paths.append(p)
```

- 对每对 `(q_entity, a_entity)` 独立搜索，结果合并到同一列表
- 同一答案实体有多条等长最短路径时，全部加入 `truth_paths`
- 不同答案实体的路径长度可能不同，没有全局统一的最短路径过滤
- 与修改后的 `convert_cokg_qa.py` 行为一致，都收集所有等长最短路径

## 最新修改（2026-03-04）

### 1. convert_cokg_qa.py 路径搜索策略优化

修改了 `find_shortest_paths_bfs` 函数（[convert_cokg_qa.py:59-150](src/gendata/convert_cokg_qa.py#L59-L150)），采用分阶段策略：

**阶段1：枚举短路径（≤2跳）**
- 显式枚举所有1跳和2跳路径，全部加入子图
- 对每个目标实体收集所有等长短路径，不做过滤

**阶段2：BFS搜索长路径（>2跳）**
- 只对阶段1未找到的目标实体执行BFS
- 使用 `visited` 集合避免重复访问，只保留一条最短路径
- 最大搜索深度为 `max_depth=5`

**改动原因**：
- 短路径（1-2跳）数量可控，枚举所有路径不会导致组合爆炸
- 长路径（>2跳）可能有大量等长路径，只保留一条避免子图过大
- 保证所有答案实体都有路径连接到问题实体

**配置变更**：
- 输入文件从 `one_hop` 改为 `three_hop`
- 输出目录从 `COKG_QA` 改为 `COKG_QA/threehop`

### 2. 新增纯路径评估函数 eval_pure_path_result

在 [qa_utils.py:578-732](src/utils/qa_utils.py#L578-L732) 新增 `eval_pure_path_result` 函数，专门评估不包含 `# Answer:` 标记的纯路径输出。

**计算指标**：
- Answer Hit / Answer Acc：路径中是否包含答案实体（子串匹配）
- Path Answer F1/Precision/Recall：路径与答案实体的匹配度
- Path F1/Precision/Recall：路径与真实路径的匹配度（如果有 `ground_truth_paths`）

**输出文件**：
- `detailed_eval_result.jsonl`：每个样本的详细评估结果
- `miss_cases.jsonl`：Answer Hit = 0 的失败案例
- `eval_result.txt`：汇总指标

**与 eval_path_result_w_ans 的区别**：
- 不解析 `# Answer:\n` 标记，直接将所有输出视为路径
- 不计算答案生成的 F1/Precision/Recall（因为没有显式答案输出）
- 更适合评估纯路径生成任务

### 3. 推理脚本切换评估函数

[predict_paths_and_answers.py:154-155](workflow/predict_paths_and_answers.py#L154-L155) 将评估函数从 `eval_path_result_w_ans` 切换为 `eval_pure_path_result`：

```python
# eval_path_result_w_ans(os.path.join(output_dir, 'predictions.jsonl'))
eval_pure_path_result(os.path.join(output_dir, 'predictions.jsonl'))
```

**原因**：当前实验输出为纯路径（不含答案标记），使用 `eval_pure_path_result` 更准确。

## 升级 transformers 后的兼容性修复（2026-03-04）

### 1. 量化参数传递方式变更

**问题**：升级 transformers 后，`Qwen3_5ForCausalLM.__init__()` 不再接受 `load_in_8bit` 和 `load_in_4bit` 参数，直接传递会报错：

```
TypeError: Qwen3_5ForCausalLM.__init__() got an unexpected keyword argument 'load_in_8bit'
```

**修复**（[base_hf_causal_model.py:6,61-76](src/llms/base_hf_causal_model.py#L6)）：

1. 导入 `BitsAndBytesConfig`：
```python
from transformers import BitsAndBytesConfig
```

2. 在 `prepare_for_inference` 中创建量化配置对象：
```python
# 配置量化参数
quantization_config = None
if self.args.quant == "8bit":
    quantization_config = BitsAndBytesConfig(load_in_8bit=True)
elif self.args.quant == "4bit":
    quantization_config = BitsAndBytesConfig(load_in_4bit=True)
```

3. 通过 `quantization_config` 参数传递：
```python
self.model = AutoModelForCausalLM.from_pretrained(
    self.args.model_path,
    quantization_config=quantization_config,  # 新方式
    # load_in_8bit=...,  # 旧方式，已移除
    # load_in_4bit=...,  # 旧方式，已移除
    ...
)
```

**原理**：新版 transformers 将量化配置封装为独立对象，避免参数污染模型初始化函数。

### 2. 缺失 generation_config.json 的兜底处理

**问题**：本地模型目录缺少 `generation_config.json` 时，代码尝试作为 PEFT 模型加载 `adapter_config.json`，但该文件也不存在，导致报错：

```
ValueError: Can't find 'adapter_config.json' at './offline_assets/models/Qwen3.5-0.8B'
```

**修复**（[base_hf_causal_model.py:91-100](src/llms/base_hf_causal_model.py#L91-L100)）：

```python
try:
    self.generation_cfg = GenerationConfig.from_pretrained(self.args.model_path)
except:
    # 尝试从 PeftModel 加载
    try:
        sft_peft_config = PeftConfig.from_pretrained(self.args.model_path)
        self.generation_cfg = GenerationConfig.from_pretrained(sft_peft_config.base_model_name_or_path)
    except:
        # 如果都失败，使用默认配置
        self.generation_cfg = GenerationConfig()
```

**原理**：三层 fallback 策略，最终兜底为默认配置，避免因配置文件缺失导致推理失败。

### 3. 约束结束后重复生成 EOS token 的问题

**问题**：Qwen3.5 模型在生成 `</PATH>` 后，`allowed_tokens_fn` 返回 `[self.tokenizer.eos_token_id]` 强制生成 EOS token，但模型会持续生成直到达到 `max_new_tokens=1024` 限制，导致输出中有大量重复的 `<|im_end|>`：

```
华为技术有限公司 -> 供应 -> 北京市公安局</PATH><|im_end|><|im_end|><|im_end|>...(重复1000+次)
```

**根本原因**：
- `prefix_allowed_tokens_fn` 只控制**生成什么**，不控制**何时停止**
- 当返回 `[eos_token_id]` 时，模型每步都生成 EOS，但生成过程不会自动停止
- 不同模型的 `generation_config` 可能有不同的停止条件，Qwen2 能正常停止，Qwen3.5 不能

**方案演进**：

**方案1（失败）**：返回空列表 `[]`
```python
if len(torch.where(sent[last_start:] == self.end_token)[0]) > 0:
    return []  # 触发生成停止
```
- **问题**：transformers 报错 `prefix_allowed_tokens_fn returned an empty list`，认为约束不可满足

**方案2（当前实现）**：使用 `StoppingCriteria`

新增 `PathEndStoppingCriteria` 类（[graph_constrained_decoding.py:5-20](src/graph_constrained_decoding.py#L5-L20)）：

```python
class PathEndStoppingCriteria(StoppingCriteria):
    """在检测到 </PATH> token 后停止生成"""
    def __init__(self, start_token_id, end_token_id):
        self.start_token_id = start_token_id
        self.end_token_id = end_token_id

    def __call__(self, input_ids: torch.LongTensor, scores: torch.FloatTensor, **kwargs) -> bool:
        # 检查每个 beam 是否都已经生成了 </PATH>
        for beam_tokens in input_ids:
            matched_start = torch.where(beam_tokens == self.start_token_id)[0]
            if len(matched_start) > 0:
                last_start = matched_start[-1]
                # 如果在最后一个 <PATH> 之后找到了 </PATH>，这个 beam 应该停止
                if len(torch.where(beam_tokens[last_start:] == self.end_token_id)[0]) == 0:
                    return False  # 还有 beam 没有结束
        return True  # 所有 beam 都已经生成了 </PATH>
```

在 `generate_sentence` 中使用（[graph_constrained_decoding_model.py:15-28](src/llms/graph_constrained_decoding_model.py#L15-L28)）：

```python
# 创建 StoppingCriteria 来在 </PATH> 后停止生成
stopping_criteria = None
if start_token_ids is not None and end_token_ids is not None:
    stopping_criteria = StoppingCriteriaList([
        PathEndStoppingCriteria(start_token_ids, end_token_ids)
    ])

res = self.model.generate(
    input_ids = input_ids,
    attention_mask = attention_mask,
    generation_config=self.generation_cfg,
    prefix_allowed_tokens_fn=gcr.allowed_tokens_fn,
    stopping_criteria=stopping_criteria,  # 新增
    return_dict_in_generate=True,
    pad_token_id=self.tokenizer.eos_token_id
)
```

**工作原理**：

1. **两个独立机制协同工作**：
   - `prefix_allowed_tokens_fn`：控制**生成什么**（约束路径 + 强制 EOS）
   - `StoppingCriteria`：控制**何时停止**（检测到 `</PATH>` 后立即停止）

2. **执行流程**（以 beam search k=2 为例）：
   ```
   Step 1: 生成路径内容
   - prefix_allowed_tokens_fn: 使用 trie 约束
   - StoppingCriteria: 检查是否有 </PATH>? 没有 → 返回 False（继续）

   Step 2: 生成 </PATH>
   - prefix_allowed_tokens_fn: 使用 trie 约束，允许生成 </PATH>
   - StoppingCriteria: 刚生成了 </PATH> → 但还需要再生成一个 token

   Step 3: 生成 EOS token
   - prefix_allowed_tokens_fn: 检测到 </PATH> 已存在 → 返回 [eos_token_id]
   - StoppingCriteria: 所有 beam 都有 </PATH>? 是 → 返回 True（立即停止）
   ```

3. **Beam Search 兼容性**：
   - 遍历所有 beam，只有当**所有 beam** 都生成了 `</PATH>` 后才停止
   - 每个 beam 独立判断，避免某个 beam 提前结束影响其他 beam

**优势**：
- `StoppingCriteria` 优先级更高，可以直接终止整个生成过程
- 只会生成**一个** EOS token，而不是重复生成
- 不会触发 transformers 的"约束不可满足"错误
- 与 beam search 完全兼容

**注意事项**：
- `StoppingCriteria` 在**每一步生成后**被调用
- 返回 `True` 会立即停止所有 beam 的生成
- 返回 `False` 继续生成下一个 token


## finetune 本次修改记录

### 1. `finetune/dataset.py`：修复 `trie=None` 导致训练全部跳过
- 问题根因：`ChinesePathGenerationWithAnswerPromptBuilder(...)` 的构造参数顺序写错了。
- `src/qa_prompt_builder.py` 中构造函数顺序是：`tokenizer, prompt, undirected, index_path_length, add_rule`。
- 原来的调用把 `index_path_length` 和 `undirected` 按位置传反，实际效果变成：
  - `undirected = 2`
  - `index_path_length = False`
- 后果：`get_graph_index()` 内部调用 `utils.dfs(..., self.index_path_length)` 时，`max_length` 变成 `0`，导致找不到任何路径，`trie` 返回 `None`。
- 连锁反应：
  - `dataset.__getitem__()` 返回 `None`
  - `collate_fn()` 过滤后整批为空
  - 训练循环里每个 batch 都被跳过
  - 最终表现为 `有效=0`、`Loss=0.0000`
- 修复方式：改成关键字传参，避免再受参数位置影响。

### 2. `finetune/train.py`：清理 `transformers` 的 dtype 弃用警告
- 将 `AutoModelForCausalLM.from_pretrained(...)` 中的 `torch_dtype=torch.bfloat16` 改为 `dtype=torch.bfloat16`。
- 这是新版 `transformers` 的推荐写法。
- 功能不变，只是去掉弃用 warning。

### 3. `finetune/train.py`：保存 LoRA 时显式保留 embedding 层
- 将：
  - `model.save_pretrained(config.output_dir)`
- 改为：
  - `model.save_pretrained(config.output_dir, save_embedding_layers=True)`
- 原因：训练脚本中新增了特殊 token：`<PATH>`、`</PATH>`，并调用了 `resize_token_embeddings(...)`。
- 这意味着 embedding 层可能已发生变化；保存 adapter 时显式包含 embedding 层更稳妥，也可避免 `peft` 关于 config / vocabulary 的 warning。

### 4. `finetune/reward.py`：修复终点实体提取错误
- 原实现使用正则：
  - `re.findall(r'->\s*([^-]+?)(?:\s*->|</PATH>|$)', path_text)`
- 这个写法会把路径中的“关系”错误地匹配成终点实体。
- 例如：
  - `华为技术有限公司 -> 供应 -> 中国移动通信集团有限公司`
  - 会被错误提取成：`供应`
- 修复后改成“按 `->` 分段解析”的思路：
  - 先去掉 `<PATH>`、`</PATH>` 以及可能追加在后面的 `Answer:` / `答案：` 内容
  - 再按 `->` 拆成 `[实体, 关系, 实体, ...]`
  - 只有当分段数为奇数时，最后一个分段才是合法终点实体
- 这样可以正确处理：
  - 单跳路径：`实体 -> 关系 -> 实体`
  - 多跳路径：`实体 -> 关系 -> 实体 -> 关系 -> 实体`
  - 不完整路径：`实体 -> 关系 ->`（这种情况直接判定为非法路径）

### 5. `finetune/reward.py`：修复 hop 统计逻辑
- 原实现用：
  - `generated_path.count('->')`
- 这个值实际上统计的是箭头数量，不等于 hop 数。
- 例如单跳路径：
  - `实体 -> 关系 -> 实体`
  - `->` 数量是 `2`，但 hop 应该是 `1`
- 修复后使用解析后的路径分段数计算：
  - `hops = (len(path_segments) - 1) // 2`
- 因而：
  - 3 段 = 1 hop
  - 5 段 = 2 hop
  - 7 段 = 3 hop

### 6. 关于日志抑制
- 中途曾临时加入过“定向日志抑制”，用于隐藏 Qwen3 的：
  - `The fast path is not available ... Falling back to torch implementation`
- 后来已按需求删除。
- 当前行为是：这条提示会正常显示，但不影响程序功能；模型仍会自动回退到 `torch` 实现。

### 7. `finetune/grpo_trainer.py`：去重后不足 K 条继续补采样
- 训练阶段继续保留 `do_sample=True` 的随机采样，不改成 beam search。
- 新增思路是：
  - 先采样一轮路径
  - 对生成文本做规范化后去重
  - 如果去重后的唯一路径数仍然小于 `num_generations`，继续补采样
- 这样做的目的不是单纯“生成更多路径”，而是尽量拿到更多**不同的候选路径**，避免一整个 group 里全是重复样本。
- 如果补采样多轮后仍然得不到足够多的唯一路径，说明当前样本在 trie 约束下本来就只有很少几条高概率合法路径，此时会直接使用现有唯一路径继续训练。

### 8. `finetune/config.py`：新增补采样轮数上限 `max_resample_rounds`
- 新增配置项：`max_resample_rounds`。
- 用来限制“去重后继续补采样”的最大轮数，防止为了凑满 K 条路径而无限重复采样。
- 当前实现里如果连续两轮都没有新增唯一路径，也会提前停止补采样。
- 这相当于在“路径多样性”和“训练时间”之间做一个折中。

### 9. `finetune/grpo_trainer.py`：为补采样后的变长序列补齐 attention mask
- 多轮补采样后，不同路径对应的生成序列长度可能不同，因此不能再直接把 `outputs.sequences` 当成天然等长的训练张量。
- 当前实现会：
  - 截断到各自真实结束位置
  - 用 `pad_sequence` 补齐到同一长度
  - 同时构造 `attention_mask`
- 后续在计算 old / new log prob 时，会结合 `attention_mask` 只统计真实 token，不把 padding 部分算进概率和损失。
- 这样可以保证“去重补采样”后，训练目标仍然是对真实生成路径做优化，而不是被 padding 干扰。

### 10. `finetune/reward.py`：语义相似度从“终点 vs 答案”改成“路径 vs 问题+答案组合”
- 之前未命中正确答案时，语义奖励只比较：
  - 路径终点实体
  - 答案实体
- 这种方式的问题是：它只关心“最后落点像不像答案”，不关心整条路径是否和问题语义一致。
- 现在改成：
  - 把生成路径整体文本作为一个语义对象
  - 把“问题 + 单个答案”拼成参考文本，例如：`问题：... 答案：...`
  - 计算“生成路径”与这些“问题+答案组合文本”的 embedding 相似度
  - 若有多个答案，则取最大相似度作为奖励基础
- 这样做的好处是：
  - 语义奖励不再只盯着终点实体
  - 会同时考虑路径内容、问题语境和答案目标之间的一致性
  - 当模型暂时还采不到正确答案时，奖励信号会更接近“这条路径是否真的像在回答这个问题”

### 11. `finetune/reward.py`：新增 embedding 缓存，减少重复请求
- 由于补采样和去重后，仍可能多次遇到相同路径文本或相同“问题+答案”参考文本。
- 当前实现新增了 `embedding_cache`：
  - 已计算过 embedding 的文本会直接复用
  - 避免重复调用嵌入 API
- 这样可以减少：
  - 网络请求次数
  - 训练阶段的 reward 计算开销
  - 重复样本带来的额外延迟

### 12. `reward` 与 `trainer` 的接口联动调整
- 由于语义相似度现在依赖“问题 + 答案”的组合文本，奖励函数接口从：
  - `calculate_group_rewards(generated_paths, a_entity, expected_hops)`
- 调整为：
  - `calculate_group_rewards(generated_paths, question, a_entity, expected_hops)`
- 因此训练器在计算 reward 时，也会把样本里的 `question` 一并传给奖励函数。
- 这个改动的本质是把 reward 从“只看答案目标”升级为“同时看问题语境和答案目标”。
